name: test-clone-actions
on: [push]
jobs:
  test-clone:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
      - run: |
          URL="https://api.github.com/repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/files"
          FILES=$(curl -s -X GET -G $URL)
          echo $FILES
